---
id: NOTE-reconciliation-batch-8-the-plan-walk-tasks-the
type: note
title: "reconciliation batch 8: the plan:walk tasks the reconciliation changed"
status: active
severity: soft
always: false
summary: The closing batch of the review, covering what the review itself changed and the pairs of items that turn out to be one job each.
summary_of: e8a67aa378d68d70
scope: []
tags:
  - v2
  - ui
  - reconciliation
  - "plan:walk"
origin: human
source_file: null
source_anchor: null
source_checksum: null
valid_from: 2026-08-25
valid_until: null
checksum: b440d77463cffc37
---

# reconciliation batch 8: the plan:walk tasks the reconciliation changed

plan:walk seq:23, 2026-08-25. plan:walk was written during the walk of 2026-08-24 and 2026-08-25, so most of its 27 open tasks needed no correction. Eleven did, and one had its central question ANSWERED.

`walk/26` -- ANSWERED, AND THE ANSWER IS NO. Preview s four carried-line disclosures and the carried item block are NOT a code gap: `preview.js` · `ctx.t('index.carriedFetch')` · ~1486 builds all five, guarded on `IndexSummary.carried`, which `read-model.ts` · `IndexSummary.carried` · ~342 resolves only on a `session-start` event with a root. The fixture is not one. This task asked to establish that before building, and it was right to.

`walk/12` -- the refusal enumeration now has a starting list from three categories: refusals RULED TO STAY (the palette s promote --all, on the approval boundary -- do not delete it; coverage s print button), refusals ALREADY OVER that nobody noticed (coverage s magnitude bar, `ctx.post`, the status strip), and SILENCES that are refusals in all but name (nine facts with no string key, `prov.projCaughtUp` which no code path can ever produce, the export format rung).

`walk/11` -- should be WIDENED from refusals to TASK BLOCKERS. Seven stale blockers were found in this one pass, each a task that named its unblocking condition in prose while the condition was quietly met.

`walk/7` -- absorbs `ui1/17c` (superseded; it asked for a ruling and got one) and `screens/3s` (refines; it is the measurement), and should be dispatched beside `ui1/17b` and `screens/1s-a` -- three read-model gaps on one request family that no parity gate can find, because all three DRAW the element and never bind it.

`walk/27` -- its own warning was right: THE ZEROES WERE AN ARTEFACT. All 21 screens have an implementing task. What survives is the distinction it draws -- a task that says what a screen IS, not what it was built from -- and its best instruction: `packs.js`, `coverage.js` and `docs.js` already open with exactly that specification, written after the fact and living in code rather than in the corpus.

`walk/28` -- must be dispatched with the AUDIT_KINDS task or that one gets closed by mistake.

`walk/20` + `builder/5`, `walk/13` + `config/1`, `walk/16` + `screens/1s-f` + `rulings/49`, `walk/24` + `port/5c` + `port/5d` + `review/6b`, `walk/25` + `port/5c` -- five pairs or groups that are one piece of work each, and in every case neither side named the other.

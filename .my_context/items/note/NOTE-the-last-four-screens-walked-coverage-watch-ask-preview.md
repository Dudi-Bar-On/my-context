---
id: NOTE-the-last-four-screens-walked-coverage-watch-ask-preview
type: note
title: "the last four screens walked: coverage, watch, ask, preview"
status: active
severity: soft
always: false
summary: The final four screens looked at one at a time, so the claim that every screen was reviewed can be checked rather than taken on trust.
summary_of: 0e6acad381ff70a0
scope: []
tags:
  - v2
  - ui
  - tree-parity
origin: human
source_file: null
source_anchor: null
source_checksum: null
valid_from: 2026-08-25
valid_until: null
checksum: c11a2bcb88504aa9
---

# the last four screens walked: coverage, watch, ask, preview

Walked 2026-08-25, one at a time, each header read and each render looked at. None needed a ruling; all four are recorded here so the walk s coverage is checkable rather than claimed.

COVERAGE -- 116 to 278 nodes, 5 findings, and it is clean. Three are the corpus being bigger (26 directory buttons against 7, 7 mono runs against 2). One is the app AHEAD with a real sentence the design lacks: `span.small` reading "no scope -- unrestricted, so nothing narrows it and it is injected on the first file touched". One is pairing noise from the first three.
Its magnitude bar was REFUSED in an earlier pass and is drawn now -- a refusal already resolved, which is the outcome plan:walk seq:12 is looking for. One refusal stands and is really a non-goal: a print button, declined because print is a stylesheet register here and "the browser s own print command reaches it without a button this screen would have to invent a word for". One thing stays OPEN in its own words: disclosing that a walk was TRUNCATED is not the same as knowing WHERE it stopped.

WATCH -- 206 to 490, 13 findings. Fifty audit rows against seven; that is the corpus. The app draws an EXTRA `line` the mockup never draws, and it is deliberate and argued: the floor the pulse columns stand on, "a measured zero and an undrawn chart are two facts and the difference has to survive". `rect` 109 to 3 is the pulse over a quiet twenty-minute window -- fixture, and the honest rendering of it. `div.tokbar` and `div.nt` absent is the known ui3 11x task: the token bar has no honest single denominator.

ASK -- 74 to 861, 10 findings, and 200 rows against the mockup s 2 explains most of the node count. `span.prop` absent is plan:walk seq:4, the PROPOSED annotation. The chips (`span.chip.ok`, `span.chip.warn`) are absent because the scene has no row in those states. The rest is placement shifted by two hundred rows.

PREVIEW -- 210 to 243, 5 findings, and the only one worth a task is below. Both sides draw FOUR cards with the same four headings; the `div.card.pane` 4-to-1 finding is a contiguous RUN, not a missing card.

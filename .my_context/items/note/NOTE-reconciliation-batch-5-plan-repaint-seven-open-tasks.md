---
id: NOTE-reconciliation-batch-5-plan-repaint-seven-open-tasks
type: note
title: "reconciliation batch 5: plan:repaint, seven open tasks verdicted"
status: active
severity: soft
always: false
summary: A fifth batch of open work read, finding a colour question posed backwards and two items still blocking work that had already been finished.
summary_of: 9a863ffcbe1535d6
acknowledged:
  - body_disagrees_with_meta@04f37f2c67c1c629
scope: []
tags:
  - v2
  - ui
  - reconciliation
  - "plan:walk"
origin: human
source_file: null
source_anchor: null
source_checksum: null
valid_from: 2026-08-25
valid_until: null
checksum: f91d7bc643002a92
---

# reconciliation batch 5: plan:repaint, seven open tasks verdicted

plan:walk seq:23, 2026-08-25.

  2w   the fonts     DONE     all three wired by ui1 Task 16; verified in three files
  3h   preserve-3d   DONE     a carrier; tasks 6 and 9 are done and heeded it
  8r   the retiming  STANDS   re-verified in code today; unchanged
  3e   the bare chip STANDS   SAME QUESTION as screens/1s-c -- dispatch together
  7b   bare glass    STANDS   RIPE: the moment it named has arrived
  13a  three hues    STANDS   one item decided by the build, one premise FALSE
  12   re-measure    STANDS   and its citation was stale by 307 lines

THE HUE FINDING IS THE ONE TO CARRY. `seq:13a` item 2 says doctor s levels "lost the RETIRED `--warn` token". `--warn` is not retired: `styles.css` · `--gold:#e8c368` · ~89 declares it beside gold, ok, carry and crit, and eight places use it. So the question is the INVERSE of the one written: the approved direction budgets FOUR meaning-hues and the shipped stylesheet has FIVE, added by implementation rather than by ruling, and no gate compares the token set against the budget.

AND `seq:13a` item 1 was decided the way it feared -- "deferred to whoever builds #pulse; it should not be decided by them alone". `watch.js` · `const KIND_HUE = {` · ~164 ships four hues plus a fifth `--faint` for a kind the build cannot name. The answer is good and argued. It still reached the owner as a fait accompli.

TWO, seq:2w and seq:3h, CLOSED ON EVIDENCE, and both had been reading as open blockers on work that had long since shipped: `2w` said the app "cannot serve the vendored fonts yet" while `static.ts` · `'.woff2': 'font/woff2',` · ~108 has carried `.woff2` since ui1 Task 16, `styles.css` is 63KB rather than the 99-byte placeholder it names, and `index.html` is 9KB rather than an empty shell.

---
id: TASK-injection-preview-the-screen-literals-ledger-names-twelve
type: task
title: "Injection preview: the screen-literals ledger names twelve unkeyed sentences as Filed, and nothing was filed"
status: active
severity: soft
always: false
summary: Twelve sentences are recorded as untranslated and as already reported, and nothing was ever reported; most of them sit on the main screen.
summary_of: 178e5e217cbed37f
scope: []
tags:
  - v2
  - ui
  - i18n
  - walk
  - "screen:preview"
  - gates
  - "plan:walk"
  - "seq:102"
  - "state:todo"
origin: agent
source_file: null
source_anchor: null
source_checksum: null
valid_from: 2026-08-29
valid_until: null
checksum: 6903e6699fee7834
plan: walk
seq: "102"
state: todo
priority: "1"
progress: "0"
needs: walk/92
source: "plan:walk seq:27, measured against test/ui/screen-literals.test.ts and screens/preview.js on 2026-08-29"
---

# Injection preview: the screen-literals ledger names twelve unkeyed sentences as Filed, and nothing was filed

WHAT THE SCREEN IS, so it can be built without opening the mockup. `nav.inj` -- **Injection preview**, `<section data-p="preview">`. The landing screen, and the one the whole visual direction is judged on. A left `.plane` of `.row`s paired to a right `.plane` of `.blk`s inside the `.pair` that carries the perspective; selecting a row holds it up and lights its paired block, and every other block drops to `.58` -- a STYLESHEET fact, so the module toggles `.sel` and never a number. Three live graphics: the six-rung GATE LADDER (every rung bound since `seenFiltered` landed 2026-08-29, one exemplar per rung, each rung carrying how many items fail at it), the SPILLED-ITEMS list (one row per `Selection.spilled` with its tier, band and price), and the FIVE-TIER BUDGET RIBBON with its ghost lane.

WHAT IT OWES: **`test/ui/screen-literals.test.ts` holds twelve user-facing sentences marked `unkeyed`, every one of them annotated "Filed" or "Filed, not accepted", and no item in this corpus files any of them.**

TEN ARE ON THIS SCREEN. In the ledger's own order: the tail of the helpbox sentence whose head is `help.p1`; the path slot on the three events that take no path; the ladder's below-the-break state `not reached -- <q>`; the ribbon tier label `<used>/<budget> - <n> in - <m> out`; the ghost tooltip `<id> - <n> tokens - budget exceeded`; the two halves of the range hint; the no-spill hint `Everything selected fit. Headroom <n> tokens.`; the index tier's hint; and the two nodes carrying the spill hint's sentence. TWO MORE are on other screens and were recorded under lane discipline rather than fixed: `coverage.js`'s `Copy failed`, and `watch.js`'s `regime change - `.

THE LEDGER IS A RATCHET AND THAT IS NOT THE SAME AS AN OWNER. Its assertion is EQUALITY in both directions, so a new literal fails because it is absent and a keyed one fails until its entry is removed -- which stops the list growing and stops it going stale. It does not make anything shrink. `plan:walk seq:60` built the check and is `done`; its own "Done when" says "whatever else the check names is either fixed or listed with a reason", and listing is what happened. So the list is the finding, correctly recorded, with nothing pointing at it.

FOUR OF THE TEN HAVE THEIR HEBREW ALREADY WRITTEN, which makes them the cheap half: the design of record's `renderRibbons` draws both languages for the no-spill hint, the spill hint's middle and its tail, and the tier label's ` in ` / ` out ` (`HEB ? ' נכנסו - ' : ' in - '`). The copy exists and needs a key, not a translator.

THREE ARE APP-ONLY PROSE with no mockup copy behind them -- the two range-hint halves and the index tier's hint -- because the design of record has no range control and no unsized-ghost state. Those need copy written in both languages, which is a different act and should be sequenced apart.

AND ONE IS A PAIR THAT MUST MOVE TOGETHER: `not reached -- <q>` substitutes `GATES[i].q`, the design of record's own gate description, which is ITSELF an unkeyed literal below the collector's two-word prose line. Keying the outer sentence alone would translate two words and leave the sentence English. The `GATES` table's descriptions go with it -- the gate CODES stay literals, correctly, as product vocabulary.

THE GATE THAT WAS CITED AS FORBIDDING THESE KEYS STOPPED FORBIDDING THEM ON 2026-08-26 -- see plan:walk seq:92, which this task waits on. `screens/preview.js` is the file that discovered that, and it keyed exactly two sentences before its bound ran out. These are the rest.

FINISH BY REMOVING THE ENTRIES, not by editing the reasons. A ledger that keeps an entry for a keyed string fails in the departed direction, which is the half that stops it becoming a graveyard.

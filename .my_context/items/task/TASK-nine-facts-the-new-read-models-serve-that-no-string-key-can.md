---
id: TASK-nine-facts-the-new-read-models-serve-that-no-string-key-can
type: task
title: nine facts the new read models serve that no string key can label
status: active
severity: soft
always: false
summary: Nine things the app already works out but has no words for, so the screens stay silent about them.
summary_of: 70ed858201d8f3f2
scope: []
tags:
  - "plan:screens"
  - "seq:10s"
  - "state:done"
  - v2
origin: human
source_file: null
source_anchor: null
source_checksum: d764f3027585502c
valid_from: 2026-08-23
valid_until: null
checksum: 66ad9a1ee3a47902
plan: screens
seq: 10s
state: done
verified_on: 2026-09-04
---

# nine facts the new read models serve that no string key can label

> The four read models built on 2026-08-23 all serve facts the screens cannot label,
> because no string key exists for them. Each was served rather than dropped, on
> `INV-nothing-is-dropped-silently`, so the data is waiting for a sentence.
>
> Template packs: `quarantined`, a real per-pack count of history rows the build
> could not validate — `imported-audit.ts`'s own words are that "a quarantine
> nobody is told about is a silent drop with extra steps"; `dropped`, the
> half-imported pack directories `readImportRecords` skips silently, so a pack with
> no `import.json` is invisible to `pack list` forever; and `missing`, a pack
> member that has left the corpus.
>
> Capture: `notGoverning`. The screen currently cannot tell a user that three
> drafts already sit in the scope they are about to file into.
>
> Composer: the glob tester's count line has no key at all — the mockup fills it from
> a hard-coded English/Hebrew ternary in its own script. The empty-result sentence,
> the one saying an item scoped there would govern nothing and that doctor calls
> `dead_scope`, cannot be said in the app.
>
> Configure: no key for a parse error, a resolve error, a refusal, or the R14.2
> skipped-keys notice, so the loader's own message is printed unworded.
>
> Every one needs a mockup change first, because `strings-parity` holds the key set
> equal to the mockup's `data-t` set in both directions.

RECONCILED 2026-08-25 under plan:walk seq:23, against the precedence order.

VERDICT: STANDS, and it is larger than it reads. Nine facts the product already COMPUTES and cannot SAY -- quarantined, dropped and missing packs; capture s notGoverning; the composer s count line and its dead_scope sentence; and four Configure states (parse error, resolve error, refusal, the R14.2 skipped-keys notice).

IT IS THE LARGEST SINGLE BLOCK OF "the engine knows and the screen cannot tell you", which is the exact shape of the owner s standing goal that a refusal is a state to leave. plan:walk seq:12 (enumerate every standing refusal and drive the list to zero) must count these nine: they are not refusals in the code s sense -- nothing 503s -- but they are silences with the same effect on a reader.

IT IS PART OF THE ONE MOCKUP SESSION. The owner s file, the owner s edit, and it can only be done beside the others: plan:walk seq:20 (draw the builder once), seq:13 (the config composer), seq:14 (carry the budget), seq:25 (the markdown route), seq:19 (foreign_store), seq:1h (Hebrew emphasis), seq:3 (a command block per procedure card), seq:6 (the simulate readout s data-t), and plan:screens seq:1s-b, seq:1s-c and seq:10s. TWELVE items, not six -- the reconciliation found six more that had been counted as ordinary open work.

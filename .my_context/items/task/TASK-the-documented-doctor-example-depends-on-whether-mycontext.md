---
id: TASK-the-documented-doctor-example-depends-on-whether-mycontext
type: task
title: the documented doctor example depends on whether mycontext is on the host's PATH, so the doc fixture leaks the host a second way
status: active
severity: soft
always: false
summary: The README's worked example for the doctor command comes out clean only on a machine where the tool is already installed on the PATH; anywhere else the same fixture prints a warning, so the documentation test fails for everyone but the owner.
summary_of: 40f09144ad5ab1fc
scope: []
tags:
  - "plan:release"
  - "seq:15"
  - "state:doing"
origin: human
source_file: null
source_anchor: null
source_checksum: null
valid_from: 2026-09-22
valid_until: null
checksum: 201b9d43c02d6fe4
plan: release
seq: "15"
state: doing
---

# the documented doctor example depends on whether mycontext is on the host's PATH, so the doc fixture leaks the host a second way

Measured on the Ubuntu job of CI run 35719545257 (commit b9c2f39d, 2026-09-22), after release/14 landed: test/docs/examples.test.ts:303 (every documented example matches what the command actually prints) and :560 (…different day) fail on the doctor block alone. The committed block says 0 findings; on the runner the fixture prints the cli_not_on_path warning (src/doctor/cli-on-path.ts), because no mycontext resolves on that machine's PATH, while on the owner's machine the global npm link does. The fixture's child (runExample in scripts/gen-doc-examples.ts) inherits the host PATH, so the documented output states a fact about the host, not about mycontext.

Closing condition: the doc fixture's child sees a PATH on which a mycontext shim resolves to THIS checkout regardless of the host (a temp bin directory holding a shim in the shape src/doctor/cli-on-path.ts readShimTarget recognises, for both the sh and .cmd spellings, prepended to a PATH from which foreign shims are already scrubbed the way test/helpers/hermetic-path.ts does); a test asserts runExampleInFixture('doctor') is byte-identical with and without a mycontext on the host PATH, proved by removal; README.md and docs/README.he.md regenerated by node scripts/gen-doc-examples.ts alone if any block moves; test/docs/examples.test.ts green on the Ubuntu job. Files: scripts/gen-doc-examples.ts, scripts/doc-fixture.ts, test/docs/examples.test.ts, README.md, docs/README.he.md. Sibling of release/12 and release/14.

# Working plan — 2026-09-06

Four subjects, taken in order, so nothing runs before it is decided. Refer to
these by number. `reports/EXECUTION-BOARD.md` stays the board; this is the
running order on top of it.

**Board at the start of this session:** 505 done · 88 open · 43 owner · 45 lane-able.
**Doctor:** 0 errors, 2 warnings. **HEAD** `1bbfa03`, 1 unpushed.

---

## §1 — Composer: analysis and debugging. DONE, nothing to dispatch.

Two agents ran. Both reported. One fix landed and is pushed.

| | result |
|---|---|
| Debugging | Root cause found and fixed. Cold load 5,291 → 2,929 ms; warm nav 2,054 → 576–815 ms |
| Architecture review | `docs/superpowers/specs/2026-09-06-composer-architecture-review.md`, 167 lines |

**What remains from §1 is four decisions, not work.** They are listed in §3 as
D1–D4. No Composer code should be written until D2 is ruled, because the
refactor as currently written would move the execution boundary.

---

## §2 — Tutorials and documentation. TWO items from closed.

13 live items: **11 done, 2 open**. 5 cancelled by the unification ruling.

| ref | state | what is left |
|---|---|---|
| `tuts/8` | doing | **18 of 24 Hebrew tutorials unwritten.** 6 written. The gap is measured, not declared |
| `docsys/4` | todo | The tutorial half only. Its README half is done; it was blocked on `tuts/5`, which landed |

Everything else in both plans is done or cancelled. The screens themselves are
finished: one Library list, documents rendered at `/doc.html` in GitHub's own
stylesheet, 0 dead links in either language.

**Decision needed:** whether to spend a lane on 18 Hebrew translations now, or
leave `tuts/8` as a measured gap. See §3, D5.

---

## §3 — On the owner. Five decisions, then 43 walk items.

Ordered by what unblocks the most.

- **D1 — `/api/status` costs ~2 s per boot and blocks the whole server.**
  `/api/doctor` is identical. Caching `runChecks` needs a staleness ruling
  because the checks read repo source. Alternative: make `checkDeadScopes`
  (660 ms) and `checkCitationForm` (320–400 ms) cheaper. **Highest value: it is
  the slowest thing in the product and it blocks every other request.**
- **D2 — the `runnable` flag.** Presence in `PALETTE` is currently the whole
  execution license. Separating "draw a form" from "let the server run it"
  moves the execution boundary. **Blocks all Composer refactor work.**
- **D3 — should a cancelled dependency unblock its dependents?**
  `buildTaskIndex` still counts deprecated tasks. Nothing is held today.
- **D4 — `builder/7` re-cut** into a safe schema move plus the per-command
  Execute ruling, and **`builder/3`'s stale "38 commands"** corrected to
  derived 43.
- **D5 — `tuts/8`:** write the 18 Hebrew tutorials now, or leave measured.
  **DONE** — 24 of 24, `9a108d3`.

### Found after the plan was written. Same treatment: decide, then dispatch.

- **D6 — `rulings/33c`, and it is what still holds the citation gate red.**
  Documents are clean — 0 moved, 0 broken — but the run exits 1 on **21 `BARE`
  faults**, 20 of them in one CLOSED campaign plan: bare `file:line` pointers
  inside fences tagged as source. The item says it must be sequenced **after** a
  ruling about what the gate checks, so it is not free work. The tool's own
  message offers the fix — drop the language tag where a block is a specimen
  rather than code in transit — but a closed campaign record is exactly the case
  nobody has ruled on.
- **D7 — the audit projection index.** `checkGoverningSpillPressure` is now the
  largest doctor check at ~190–222 ms, and the diagnosis is exact: `topItems`
  filters on `role` while `idx_audit_item_id` leads with `item_id`, so the join
  cannot seek and SQLite scans it. One line in the schema block — but it is a
  **projection schema change** behind a version check and a rebuild path.
- **D8 — the RTL convention.** The two Hebrew families solve right-to-left in
  completely disjoint ways: the tutorials use **343** RLM marks and **246**
  non-breaking hyphens and **zero** `dir=` attributes; `docs/README.he.md` uses
  **1,860** `dir=` attributes and neither mark. Both legitimate, both now served
  by the same viewer, and nothing says which this project uses.
  `OPENQ-the-hebrew-tutorials-and-the-hebrew-readme-solve-right-to`.
- **D9 — `<ins>` and `<del>` as run markers**, the one live question left from
  `walk/16`. Small, unblocked, nothing waits on it.
  `OPENQ-do-the-ins-and-del-marks-become-run-markers`.
- **D10 — four Composer fields gain a picker.** `tags` (218 free + 3 projected,
  via `/api/tags`), `status` (5, closed), `relation` (18, closed), `topic` (7).
  Chosen because every domain is small or already split, so none needs the
  picker primitive to change first. Modelled on the focus dialog, which already
  answers the hard half — free-form tags as checkboxes, a projected prefix as
  one `<select>` ("pick a plan, never 217 checkboxes"), and the composed line
  as the source of truth. **RULED, DISPATCHED** · `plan:builder seq:9`.
- **D11 — three more fields, and a picker that can hold nine hundred entries.**
  `finding` (doctor's live findings), `key` (the staged lesson's own), `pack`
  (packs on disk). Held behind one ruling: does the builder keep a plain
  `<select>` for a long domain, or gain a searchable combobox? The `id` picker
  builds **938** options today and rebuilds them on every command switch.
  Deferring costs nothing — `pickerOptions()` returns a plain array, so the
  combobox is a drop-in and nothing built in D10 has to be undone.
  **Dispatch after D10** · `plan:builder seq:10`, `needs: builder/9`.
- **D12 — the Composer tested as a user, exhaustively.** The tools plan the
  tests, cover every field, value and interacting combination, nothing skipped;
  then the plan is executed and every failure fixed by systematic debugging
  until it passes. Surface: 30 entries, 89 fields, 21 pickers, 8 option sets,
  60 free-text, 3 compose-but-never-run.

  **A test passes only once the command has been EXECUTED and returned the
  correct result** — owner, same day. A composed line that looks right is not a
  passing test. That raises the task: **19 of the 30 entries are writes**, so
  this mutates a corpus, and **the scratch-corpus question is a prerequisite,
  not a detail** — the standing rule is that tests run against the current
  corpus and an exception needs his approval first. Two further consequences:
  for the three `runnable: false` entries the **correct result is the refusal**,
  and a test that executes one successfully is a security regression rather than
  a pass; and 16 of the 27 runnable entries sit on the approval boundary, so
  executing them exercises the confirm-and-nonce path, which must not be
  bypassed to make a test convenient.

  **Dispatch only after every other Composer task** — D10, D11, and
  `builder/4`, `/5`, `/6`, `/8` · `plan:builder seq:11`,
  `needs: builder/9, builder/10`.

### Carried, not decisions — work released by the above

`builder/4`, `/5`, `/6`, `/8` are the actual Composer refactor and D2 unblocked
them. `builder/5` cites `port/95` as a blocker; `port/95` is recorded `done`, so
that citation should be re-checked rather than believed.

One literal survives the `runnable` work: `src/doctor/checks.ts` hand-writes
`AUDIT_FILES`, and its docblock now states a false premise — it says `PALETTE`
carries no `audit` entry, which stopped being true in `3702b1a`.

**And the 43 `walk` items**, which close the way the four did on 2026-09-05:
driven in a browser, shown, recommended, ruled.

---

## §4 — Dispatch, after §3 and only what §3 released.

Nothing dispatches before its decision exists. Standing constraints on every
lane, because two were broken yesterday:

- no git command that writes — a worker reverted three corpus edits and reported
  nothing was lost;
- never restart the server on 58888 — `ui-server-upkeep` respawns it, so a
  restart is never a worker's to perform;
- `docs/design/web-ui-mockup.html` is frozen, not one byte;
- a UI change is not done until a browser test drives it.

**Not dispatched and waiting on a ruling of its own:** `rulings/33c`. The
citation gate still exits 1 on **21 `BARE` faults**, 20 in one closed campaign
plan. That item says it must be sequenced after a ruling about what the gate
checks, so it is not free work.

// harness/cases/cli-retrieve.mjs
const SEED = ['add', 'constraint', 'Pool capped at 20', '--scope', 'src/db/**', '--tags', 'db', '--yes'];
const seed = [SEED];

// Richer corpus for retrieval commands that need distinguishable filters
const CORPUS = [
  ['add', 'constraint', 'Pool capped at 20', '--scope', 'src/db/**', '--tags', 'db,perf', '--severity', 'hard', '--yes'],
  ['add', 'constraint', 'Uploads capped at 10 MB', '--scope', 'src/api/**', '--tags', 'api', '--yes'],
  ['add', 'rule', 'Write the failing test first', '--tags', 'testing', '--yes'],
  ['add', 'invariant', 'Prices are integer cents', '--scope', 'src/billing/**', '--tags', 'billing', '--yes'],
  ['add', 'decision', 'We chose Stripe for settlement timing', '--tags', 'billing'],
  ['add', 'lesson', 'Retry storms need jitter'],
];

export const cases = [
  // --- search ---
  { id: 'search-positional', kind: 'cli', setup: seed, argv: ['search', 'pool'] },
  { id: 'search-text-flag', kind: 'cli', setup: seed, argv: ['search', '--text', 'pool'] },
  { id: 'search-both-forms-refused', kind: 'cli', setup: seed,
    argv: ['search', 'pool', '--text', 'pool'], note: 'bare positional and --text together refused' },
  { id: 'search-no-filter-refused', kind: 'cli', setup: seed, argv: ['search'],
    note: 'at least one filter required' },
  { id: 'search-type', kind: 'cli', setup: seed, argv: ['search', '--type', 'constraint'] },
  { id: 'search-tag', kind: 'cli', setup: seed, argv: ['search', '--tag', 'db'] },
  { id: 'search-path', kind: 'cli', setup: seed, argv: ['search', '--path', 'src/db/writer.ts'] },
  { id: 'search-status', kind: 'cli', setup: seed, argv: ['search', '--status', 'active'] },
  { id: 'search-relation', kind: 'cli', setup: seed, argv: ['search', '--relation', 'relates_to'] },
  { id: 'search-relation-invalid', kind: 'cli', setup: seed, argv: ['search', '--relation', 'nope'] },
  { id: 'search-limit', kind: 'cli', setup: seed, argv: ['search', '--type', 'constraint', '--limit', '1'] },
  { id: 'search-json', kind: 'cli', setup: seed, argv: ['search', '--type', 'constraint', '--json'] },
  { id: 'search-unknown-flag', kind: 'cli', setup: seed, argv: ['search', 'pool', '--nope'] },

  // --- query ---
  { id: 'query-select', kind: 'cli', setup: seed,
    argv: ['query', 'SELECT id, type FROM items ORDER BY id'] },
  { id: 'query-json', kind: 'cli', setup: seed,
    argv: ['query', 'SELECT id FROM items', '--json'],
    note: 'shape must be {rows,rowCount,truncated,limit,loadErrors}' },
  { id: 'query-limit', kind: 'cli', setup: seed, argv: ['query', 'SELECT id FROM items', '--limit', '1'] },
  { id: 'query-limit-zero-refused', kind: 'cli', setup: seed,
    argv: ['query', 'SELECT id FROM items', '--limit', '0'], note: 'minimum is 1' },
  { id: 'query-detail-flag-rejected', kind: 'cli', setup: seed,
    argv: ['query', 'SELECT id FROM items', '--full'],
    note: 'query.ts:189 rejects detail flags' },
  { id: 'query-insert-refused', kind: 'cli', setup: seed,
    argv: ['query', "INSERT INTO items VALUES ('x')"] },
  { id: 'query-drop-refused', kind: 'cli', setup: seed, argv: ['query', 'DROP TABLE items'] },
  { id: 'query-pragma-refused', kind: 'cli', setup: seed, argv: ['query', 'PRAGMA table_info(items)'] },
  { id: 'query-with-cte-allowed', kind: 'cli', setup: seed,
    argv: ['query', 'WITH x AS (SELECT id FROM items) SELECT * FROM x'] },
  { id: 'query-ledger-missing-table', kind: 'cli', setup: [SEED, ['rebuild']],
    argv: ['query', 'SELECT * FROM ledger'],
    note: 'README 2551: rebuild creates no ledger tables' },
  { id: 'query-double-dash-separator', kind: 'cli', setup: seed,
    argv: ['query', '--', '-- a comment\nSELECT id FROM items'] },

  // --- audit: every one of the 12 flags ---
  { id: 'audit-bare', kind: 'cli', setup: CORPUS, argv: ['audit'] },
  { id: 'audit-since-span', kind: 'cli', setup: CORPUS, argv: ['audit', '--since', '7d'] },
  { id: 'audit-since-hours', kind: 'cli', setup: CORPUS, argv: ['audit', '--since', '12h'] },
  { id: 'audit-since-iso', kind: 'cli', setup: CORPUS, argv: ['audit', '--since', '2026-08-01'] },
  { id: 'audit-until', kind: 'cli', setup: CORPUS, argv: ['audit', '--until', '2026-12-31'],
    note: 'UNDOCUMENTED: appears zero times in README' },
  { id: 'audit-item', kind: 'cli', setup: CORPUS, argv: ['audit', '--item', 'CONST-pool-capped-at-20'] },
  { id: 'audit-session', kind: 'cli', setup: CORPUS, argv: ['audit', '--session', 'abc'] },
  { id: 'audit-kind-injection', kind: 'cli', setup: CORPUS, argv: ['audit', '--kind', 'injection'] },
  { id: 'audit-kind-focus', kind: 'cli', setup: CORPUS, argv: ['audit', '--kind', 'focus'] },
  { id: 'audit-kind-invalid', kind: 'cli', setup: CORPUS, argv: ['audit', '--kind', 'nope'] },
  { id: 'audit-op-create', kind: 'cli', setup: CORPUS, argv: ['audit', '--op', 'create'] },
  { id: 'audit-op-invalid', kind: 'cli', setup: CORPUS, argv: ['audit', '--op', 'nope'] },
  { id: 'audit-origin-agent', kind: 'cli', setup: CORPUS, argv: ['audit', '--origin', 'agent'] },
  { id: 'audit-role', kind: 'cli', setup: CORPUS, argv: ['audit', '--role', 'human'],
    note: 'UNDOCUMENTED: accepted at audit.ts:25, absent from USAGE and README' },
  { id: 'audit-limit', kind: 'cli', setup: CORPUS, argv: ['audit', '--limit', '5'] },
  { id: 'audit-summary', kind: 'cli', setup: CORPUS, argv: ['audit', '--summary'] },
  { id: 'audit-items', kind: 'cli', setup: CORPUS, argv: ['audit', '--items'] },
  { id: 'audit-sessions', kind: 'cli', setup: CORPUS, argv: ['audit', '--sessions'] },
  { id: 'audit-files', kind: 'cli', setup: CORPUS, argv: ['audit', '--files'] },
  { id: 'audit-json', kind: 'cli', setup: CORPUS, argv: ['audit', '--json'] },
  { id: 'audit-replay-ledger', kind: 'cli', setup: CORPUS, argv: ['audit', 'replay-ledger'],
    note: 'hidden positional subcommand at audit.ts:230' },
  { id: 'audit-unknown-flag', kind: 'cli', setup: CORPUS, argv: ['audit', '--nope'] },
  { id: 'audit-until-past', kind: 'cli', setup: CORPUS, argv: ['audit', '--until', '2020-01-01'],
    note: 'GAP 1: every corpus record postdates 2020-01-01; if --until works, result must be empty' },
  { id: 'audit-since-future', kind: 'cli', setup: CORPUS, argv: ['audit', '--since', '2099-01-01'],
    note: 'GAP 1: every corpus record predates 2099-01-01; if --since works, result must be empty' },
  { id: 'audit-op-create-discriminates', kind: 'cli',
    setup: [...CORPUS, ['pin', 'CONST-pool-capped-at-20', '--yes']],
    argv: ['audit', '--op', 'create'],
    note: 'GAP 2: corpus setup creates 6 items; pin mutation adds an update op; filtering --op create should exclude the pin' },
  { id: 'audit-op-update-discriminates', kind: 'cli',
    setup: [...CORPUS, ['pin', 'CONST-pool-capped-at-20', '--yes']],
    argv: ['audit', '--op', 'update'],
    note: 'GAP 2: corpus setup creates 6 items; pin mutation adds an update op; filtering --op update should return only the pin' },
  { id: 'audit-items-role-subject', kind: 'cli', setup: CORPUS, argv: ['audit', '--items', '--role', 'subject'],
    note: 'GAP 4: --role is only read inside --items branch; valid values are subject, injected, spilled' },
  { id: 'audit-items-role-injected', kind: 'cli', setup: CORPUS, argv: ['audit', '--items', '--role', 'injected'],
    note: 'GAP 4: --role is only read inside --items branch; valid values are subject, injected, spilled' },
  { id: 'audit-role-garbage', kind: 'cli', setup: CORPUS, argv: ['audit', '--role', 'nonsense'],
    note: 'GAP 4: sibling flags --kind and --op reject invalid values with "must be one of" error; testing whether --role does the same' },

  // --- decay ---
  { id: 'decay-bare', kind: 'cli', setup: CORPUS, argv: ['decay'] },
  { id: 'decay-sessions', kind: 'cli', setup: CORPUS, argv: ['decay', '--sessions', '5'] },
  { id: 'decay-sessions-zero-refused', kind: 'cli', setup: CORPUS, argv: ['decay', '--sessions', '0'] },
  { id: 'decay-all', kind: 'cli', setup: CORPUS, argv: ['decay', '--all'] },
  { id: 'decay-summary-still-prints-caveat', kind: 'cli', setup: CORPUS, argv: ['decay', '--summary'],
    note: 'README 2273: caveat prints at every detail level' },
  { id: 'decay-json', kind: 'cli', setup: CORPUS, argv: ['decay', '--json'] },
  { id: 'decay-unknown-flag', kind: 'cli', setup: CORPUS, argv: ['decay', '--nope'] },

  // --- focus ---
  { id: 'focus-bare-reports', kind: 'cli', setup: CORPUS, argv: ['focus'] },
  { id: 'focus-positional-tag', kind: 'cli', setup: CORPUS, argv: ['focus', 'db'] },
  { id: 'focus-two-tags', kind: 'cli', setup: CORPUS, argv: ['focus', 'db', 'api'] },
  { id: 'focus-tag-flag', kind: 'cli', setup: CORPUS, argv: ['focus', '--tag', 'db'],
    note: 'advertised in commands/focus.md, absent from README' },
  { id: 'focus-category', kind: 'cli', setup: CORPUS, argv: ['focus', '--category', 'constraint'] },
  { id: 'focus-scope-path', kind: 'cli', setup: CORPUS, argv: ['focus', '--scope', 'src/db/writer.ts'] },
  { id: 'focus-scope-glob', kind: 'cli', setup: CORPUS, argv: ['focus', '--scope', 'src/db/**'] },
  { id: 'focus-preview', kind: 'cli', setup: CORPUS, argv: ['focus', 'db', '--preview'] },
  { id: 'focus-show', kind: 'cli', setup: CORPUS, argv: ['focus', '--show'],
    note: 'real per focus.ts:43, under-documented' },
  { id: 'focus-relations', kind: 'cli', setup: CORPUS, argv: ['focus', '--relations'] },
  { id: 'focus-json', kind: 'cli', setup: CORPUS, argv: ['focus', '--json'] },
  { id: 'focus-clear', kind: 'cli', setup: CORPUS, argv: ['focus', '--clear'] },
  { id: 'focus-clear-with-axis-refused', kind: 'cli', setup: CORPUS,
    argv: ['focus', '--clear', '--tag', 'db'], note: 'focus.ts:108 refuses the combination' },
  { id: 'focus-hard-item-never-hidden', kind: 'cli',
    setup: [['add', 'constraint', 'Never commit a secret', '--severity', 'hard', '--yes'], ...CORPUS],
    argv: ['focus', 'db', '--preview'],
    note: 'README 2484: focus never hides a severity:hard item' },
  { id: 'focus-show-with-active', kind: 'cli', setup: [...CORPUS, ['focus', 'db']], argv: ['focus', '--show'],
    note: 'GAP 3: contrast with focus-show empty state; this observes active focus' },
  { id: 'focus-json-with-active', kind: 'cli', setup: [...CORPUS, ['focus', 'db']], argv: ['focus', '--json'],
    note: 'GAP 3: contrast with focus-json empty state; this observes active focus' },
  { id: 'focus-clear-with-active', kind: 'cli', setup: [...CORPUS, ['focus', 'db']], argv: ['focus', '--clear'],
    note: 'GAP 3: contrast with focus-clear empty state; this clears an active focus' },
  { id: 'focus-bare-with-active', kind: 'cli', setup: [...CORPUS, ['focus', 'db']], argv: ['focus'],
    note: 'GAP 3: contrast with focus-bare-reports empty state; this reports on active focus' },
];
